<div class="container">
   <div class="col-md-12">
      <div class="row">
         <hr>
         <h4><%= @story.body %></h4>
         <hr>
         <div class="pull-right">
            <small>Posted on <%= @story.created_at.strftime('%B %d, %Y at %l:%M %p') %></small>
            <br>
            <small>by <%= @story.user.name %></small>
         </div>
      </div>
      <div class="row">
         <hr>

      </div>
   </div>
</div>
